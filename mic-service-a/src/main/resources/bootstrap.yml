spring:
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      enabled: false
      allow-override: true
  application:
    name: mic-service-a



server:
  port: 9001


management:
  endpoints:
    web:
      exposure:
        include: "*"